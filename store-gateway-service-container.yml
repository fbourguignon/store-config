server:
  port: 9000

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://eureka-server:9091/eureka/

zuul:
  prefix: /store
  ignoredServices: '*'
  routes:
    store-product-service:
      path: /products/**
      url: http://store-product-service:9093/products	
      strip-prefix: false	